<%= form_for(product) do |f| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title,:class => "w3-input"   %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description,:class => "w3-input"  %>
  </div>

  <div class="field">
    <%= f.label :image_url %>
    <%= f.text_field :image_url,:class => "w3-input"  %>
  </div>

  <div class="field">
    <%= f.label :price %>
    <%= f.number_field :price,:class => "w3-input"  %>
  </div>

  <div class="field">
    <%= f.label :category %>
    <%= f.text_field :category,:class => "w3-input" %>
  </div>
  <br>

  <div class="actions">
    <%= f.submit :class => "w3-btn" %>
  </div>
    <br>
     <%= link_to "View products", products_path,:class => "w3-btn" %> 
  <br>

<% end %>

